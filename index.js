// This is a flag to indicate wethere we find what we are looking for.
let hasBlue = false;

const colors = ['red', 'yellow', 'blue', 'green'];

// A sample solution using for loops might look like this.
// for (let color of colors) {
//   if (color === 'blue') {
//     hasBlue = true;
//     break;
//   }
// }

// Create the same result as the above function. Meaning you will change the flag to true if array have the color blue.
